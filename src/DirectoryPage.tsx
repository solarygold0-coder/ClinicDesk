import{FormEvent,useEffect,useState}from'react';import{Building2,Stethoscope,Plus}from'lucide-react';import{api}from'./api';
export function DirectoryPage(){const[clinics,setClinics]=useState<any[]>([]),[doctors,setDoctors]=useState<any[]>([]),[clinicName,setClinicName]=useState(''),[doctorName,setDoctorName]=useState(''),[clinicId,setClinicId]=useState(''),[error,setError]=useState('');async function load(){try{const[c,d]=await Promise.all([api.clinics(),api.doctors()]);setClinics(c);setDoctors(d);setError('')}catch(e){setError(String(e))}}useEffect(()=>{void load()},[]);async function addClinic(e:FormEvent){e.preventDefault();try{await api.createClinic({name:clinicName});setClinicName('');await load()}catch(e){setError(String(e))}}async function addDoctor(e:FormEvent){e.preventDefault();try{await api.createDoctor({name:doctorName,clinicId:clinicId?Number(clinicId):null});setDoctorName('');setClinicId('');await load()}catch(e){setError(String(e))}}return <section className="page"><div className="pageTitle"><div><h1>العيادات والأطباء</h1><p>ابدأ فارغًا وأضف بيانات منشأتك فقط.</p></div></div>{error&&<div className="error">{error}</div>}<div className="directoryGrid"><div className="tableCard"><div className="cardHead"><Building2/><div><h2>العيادات</h2><p>{clinics.length} عيادة</p></div></div><form className="inlineAdd" onSubmit={addClinic}><input value={clinicName} onChange={e=>setClinicName(e.target.value)} placeholder="اسم العيادة" required/><button className="primary"><Plus/>إضافة</button></form>{clinics.length===0?<div className="miniEmpty">لا توجد عيادات حتى الآن.</div>:<div className="simpleList">{clinics.map(c=><div key={c.id}><Building2/><b>{c.name}</b></div>)}</div>}</div><div className="tableCard"><div className="cardHead"><Stethoscope/><div><h2>الأطباء</h2><p>{doctors.length} طبيب</p></div></div><form className="inlineAdd stack" onSubmit={addDoctor}><input value={doctorName} onChange={e=>setDoctorName(e.target.value)} placeholder="اسم الطبيب" required/><select value={clinicId} onChange={e=>setClinicId(e.target.value)}><option value="">بدون عيادة محددة</option>{clinics.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><button className="primary"><Plus/>إضافة طبيب</button></form>{doctors.length===0?<div className="miniEmpty">لا يوجد أطباء حتى الآن.</div>:<div className="simpleList">{doctors.map(d=><div key={d.id}><Stethoscope/><b>{d.name}</b><small>{clinics.find(c=>c.id===d.clinicId)?.name||'بدون عيادة'}</small></div>)}</div>}</div></div></section>}
