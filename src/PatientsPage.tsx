import{FormEvent,useEffect,useState}from'react';import{Search,UserPlus,X,Trash2,Pencil,FileText,Phone,IdCard,CalendarDays,Clock3}from'lucide-react';import{api,Appointment,Patient,PatientInput}from'./api';import{PatientAttachments}from'./PatientAttachments';const blank:PatientInput={fullName:'',nationalId:'',phone:'',birthDate:'',sex:'',medicalSummary:'',chronicDiseases:'',allergies:'',notes:''};const status:Record<string,string>={scheduled:'مجدول',arrived:'وصل',in_progress:'قيد الخدمة',completed:'مكتمل',cancelled:'ملغي',no_show:'لم يحضر'};
export function PatientsPage({initialFileNo,onInitialHandled}:{initialFileNo?:number|null;onInitialHandled?:()=>void}){const[rows,setRows]=useState<Patient[]>([]),[q,setQ]=useState(''),[open,setOpen]=useState(false),[details,setDetails]=useState<Patient|null>(null),[history,setHistory]=useState<Appointment[]>([]),[historyBusy,setHistoryBusy]=useState(false),[editing,setEditing]=useState<Patient|null>(null),[form,setForm]=useState<PatientInput>(blank),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function load(query=q){try{setRows(await api.patients(query));setError('')}catch(e){setError(String(e))}}async function show(p:Patient){setDetails(p);setHistory([]);setHistoryBusy(true);try{setHistory(await api.patientAppointments(p.id,30))}catch(e){setError(String(e))}finally{setHistoryBusy(false)}}useEffect(()=>{if(initialFileNo!=null){const query=String(initialFileNo);setQ(query);void api.patientByFileNo(initialFileNo).then(p=>{if(p){setRows([p]);setError('');void show(p)}else{setRows([]);setError('ملف المريض غير موجود')}}).catch(e=>setError(String(e))).finally(()=>onInitialHandled?.());return}void load('')},[initialFileNo]);function add(){setEditing(null);setForm(blank);setError('');setOpen(true)}function edit(p:Patient){setEditing(p);setForm({fullName:p.fullName,nationalId:p.nationalId||'',phone:p.phone||'',birthDate:p.birthDate||'',sex:p.sex||'',medicalSummary:p.medicalSummary||'',chronicDiseases:p.chronicDiseases||'',allergies:p.allergies||'',notes:p.notes||''});setError('');setDetails(null);setOpen(true)}async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{editing?await api.updatePatient(editing.id,form):await api.createPatient(form);setOpen(false);setEditing(null);setForm(blank);await load()}catch(e){setError(String(e))}finally{setBusy(false)}}async function remove(p:Patient){try{const future=await api.patientFutureAppointmentCount(p.id);if(future>0){setError(`لا يمكن حذف ملف ${p.fullName}: لديه ${future} موعد/مواعيد مستقبلية نشطة. ألغِ المواعيد أولاً.`);return}if(!confirm(`حذف ملف المريض ${p.fullName}؟ سيبقى رقم الملف محفوظًا ولن يعاد استخدامه.`))return;await api.deletePatient(p.id);setDetails(null);await load()}catch(e){setError(String(e))}}return <section className="page"><div className="pageTitle"><div><h1>المرضى</h1><p>إدارة ملفات المرضى والبحث السريع</p></div><button className="primary" onClick={add}><UserPlus/>إضافة مريض</button></div><div className="searchbar"><Search/><input value={q} onChange={e=>{setQ(e.target.value);void load(e.target.value)}} placeholder="ابحث بالاسم، رقم الملف، الهوية أو الجوال"/><span>{q?`${rows.length} نتيجة`:`${rows.length} ملف`}</span></div>{error&&!open&&<div className="error">{error}</div>}<div className="tableCard">{rows.length===0?<div className="empty"><UserPlus/><h3>{q?'لا توجد نتائج مطابقة':'لا توجد ملفات مرضى'}</h3><p>{q?'جرّب الاسم أو رقم الملف أو الهوية أو الجوال.':'أضف أول مريض للبدء.'}</p>{!q&&<button className="primary" onClick={add}>إضافة مريض</button>}</div>:<table><thead><tr><th>رقم الملف</th><th>اسم المريض</th><th>رقم الهوية</th><th>الجوال</th><th>إجراءات</th></tr></thead><tbody>{rows.map(p=><tr key={p.id} className="clickableRow" onDoubleClick={()=>void show(p)}><td><bdi>{p.fileNo}</bdi></td><td><button className="patientNameLink" onClick={()=>void show(p)}>{p.fullName}</button></td><td><bdi>{p.nationalId||'—'}</bdi></td><td><bdi>{p.phone||'—'}</bdi></td><td><div className="rowActions"><button aria-label="فتح الملف" title="فتح الملف" onClick={()=>void show(p)}><FileText/></button><button aria-label="تعديل" title="تعديل" onClick={()=>edit(p)}><Pencil/></button><button className="dangerIcon" aria-label="حذف" title="حذف" onClick={()=>void remove(p)}><Trash2/></button></div></td></tr>)}</tbody></table>}</div>{details&&<div className="modalBackdrop" onMouseDown={e=>e.target===e.currentTarget&&setDetails(null)}><div className="modal patientRecord"><div className="modalHead"><div><h2>{details.fullName}</h2><p>ملف المريض رقم <bdi>{details.fileNo}</bdi></p></div><button type="button" className="icon" onClick={()=>setDetails(null)}><X/></button></div><div className="recordGrid"><article><IdCard/><span><small>رقم الهوية</small><strong dir="ltr">{details.nationalId||'غير مسجل'}</strong></span></article><article><Phone/><span><small>رقم الجوال</small><strong dir="ltr">{details.phone||'غير مسجل'}</strong></span></article><article><CalendarDays/><span><small>تاريخ الميلاد</small><strong dir="ltr">{details.birthDate||'غير مسجل'}</strong></span></article><article><UserPlus/><span><small>الجنس</small><strong>{details.sex||'غير محدد'}</strong></span></article></div><section className="recordSummary"><h3>المعلومات الطبية المختصرة</h3><p>{details.medicalSummary||'لا توجد معلومات طبية مختصرة مسجلة.'}</p></section><section className="recordSummary"><h3>الأمراض المزمنة</h3><p>{details.chronicDiseases||'لا توجد أمراض مزمنة مسجلة.'}</p></section><section className="recordSummary"><h3>الحساسية</h3><p>{details.allergies||'لا توجد حساسية مسجلة.'}</p></section><section className="recordSummary"><h3>ملاحظات</h3><p>{details.notes||'لا توجد ملاحظات مسجلة.'}</p></section><PatientAttachments patientId={details.id}/><section className="recordSummary"><h3><CalendarDays/> سجل المواعيد</h3>{historyBusy?<p>جارٍ تحميل المواعيد…</p>:history.length===0?<p>لا توجد مواعيد مسجلة لهذا المريض.</p>:<div className="patientHistory">{history.map(a=><article key={a.id}><Clock3/><span><strong>{new Date(a.startsAt).toLocaleString('ar-SA',{dateStyle:'medium',timeStyle:'short'})}</strong><small>{a.clinicName||'بدون عيادة'}{a.doctorName?` • ${a.doctorName}`:''}</small></span><em className={`status status-${a.status}`}>{status[a.status]||a.status}</em></article>)}</div>}</section><div className="modalActions"><button className="danger" onClick={()=>void remove(details)}><Trash2/>حذف الملف</button><button className="primary" onClick={()=>edit(details)}><Pencil/>تعديل البيانات</button></div></div></div>}{open&&<div className="modalBackdrop" onMouseDown={e=>e.target===e.currentTarget&&setOpen(false)}><form className="modal" onSubmit={submit}><div className="modalHead"><div><h2>{editing?'تعديل بيانات المريض':'إضافة مريض'}</h2><p>{editing?`رقم الملف ${editing.fileNo} ثابت ولا يتغير.`:'سيُنشأ رقم الملف تلقائيًا بالتسلسل.'}</p></div><button type="button" className="icon" onClick={()=>setOpen(false)}><X/></button></div><div className="formGrid"><label className="full">اسم المريض<span>*</span><input autoFocus value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})} required/></label><label>رقم الهوية<input inputMode="numeric" dir="ltr" value={form.nationalId} onChange={e=>setForm({...form,nationalId:e.target.value.replace(/[^0-9٠-٩]/g,'')})} maxLength={10}/></label><label>رقم الجوال<input inputMode="tel" dir="ltr" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>تاريخ الميلاد<input type="date" value={form.birthDate} onChange={e=>setForm({...form,birthDate:e.target.value})}/></label><label>الجنس<select value={form.sex} onChange={e=>setForm({...form,sex:e.target.value})}><option value="">غير محدد</option><option>ذكر</option><option>أنثى</option></select></label><label className="full">معلومات طبية مختصرة<textarea value={form.medicalSummary} onChange={e=>setForm({...form,medicalSummary:e.target.value})}/></label><label className="full">الأمراض المزمنة<textarea value={form.chronicDiseases} onChange={e=>setForm({...form,chronicDiseases:e.target.value})} placeholder="مثال: السكري، ضغط الدم"/></label><label className="full">الحساسية<textarea value={form.allergies} onChange={e=>setForm({...form,allergies:e.target.value})} placeholder="مثال: حساسية البنسلين"/></label><label className="full">ملاحظات<textarea value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></label></div>{error&&<div className="error modalError">{error}</div>}<div className="modalActions"><button type="button" onClick={()=>setOpen(false)}>إلغاء</button><button className="primary" disabled={busy}>{busy?'جارٍ الحفظ…':editing?'حفظ التعديلات':'حفظ المريض'}</button></div></form></div>}</section>}
