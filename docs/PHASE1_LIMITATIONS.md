# Phase 1 limitations

The dashboard currently demonstrates layout/RTL/empty states only. Its buttons are not yet connected to CRUD/routing and counts are placeholders. This is deliberate: persistence APIs are added only after the new Tauri/Rust/React foundation passes compilation and tests.
