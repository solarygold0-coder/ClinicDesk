# Phase 2 review gate

Do not begin scheduling until Windows CI is green. Review patient numbering, duplicate normalization, soft deletion, empty directory first-run state, absence of startup authentication/admin, RTL inheritance, and Rust-only database access. Failed checks must be corrected rather than bypassed.
